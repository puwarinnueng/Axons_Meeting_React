
const MainContainer = (props) => {
    return (
        <div className="main-container">
        {/* <div className=""> */}
            {props.children}
        </div>
    );
}

export default MainContainer;